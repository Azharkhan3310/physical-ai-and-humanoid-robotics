module.exports = {
  // Print width
  printWidth: 100,
  // Tab width
  tabWidth: 2,
  // Use tabs instead of spaces
  useTabs: false,
  // Print semicolons at the ends of statements.
  semi: true,
  // Use single quotes instead of double quotes.
  singleQuote: true,
  // Print trailing commas wherever possible.
  trailingComma: 'es5',
  // Print spaces between brackets in object literals.
  bracketSpacing: true,
  // Put the > of a multi-line HTML (HTML, JSX, Vue, Angular) element at the end of the last line instead of being alone on the next line.
  bracketSameLine: false,
  // Avoid wrapping only paren-less arrow functions.
  arrowParens: 'always',
  // Enforce consistent newlines for HTML attributes.
  htmlWhitespaceSensitivity: 'css',
  // Newline for JSX elements.
  jsxSingleQuote: false,
  // The line endings.
  endOfLine: 'lf',
};
